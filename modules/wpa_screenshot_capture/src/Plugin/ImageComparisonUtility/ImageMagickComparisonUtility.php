<?php

namespace Drupal\wpa_screenshot_capture\Plugin\ImageComparisonUtility;

use Drupal\wpa_screenshot_capture\Plugin\ImageComparisonUtilityBase;

/**
 * Captures screenshot of a remote uri.
 *
 * @ImageComparisonUtility(
 *   id = "wpa_imagemagick_compare",
 *   label = @Translation("ImageMagick", context = "Web Page Archive"),
 *   description = @Translation("Compares images and generates diff images.", context = "Web Page Archive")
 * )
 */
class ImageMagickComparisonUtility extends ImageComparisonUtilityBase {
  // TODO: Add functionality.
}
